const fruit = ['apple', 'banana', 'orange', 'banana']

const bananaAt = fruit.indexOf('banana', 2)
console.log(bananaAt);

//=============================================================

const bananaAt1 = fruit.lastIndexOf('banana', 2)
console.log(bananaAt1);