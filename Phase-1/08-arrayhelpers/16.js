const arr = [12, 32, 54, 96]
const index = arr.findIndex((ele) => {
    if (ele > 50) return true
})
console.log(index);


//indexOf
const findofindex = arr.indexOf(54)
console.log(findofindex);

//============================================================

